@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model MovieForum.Models.Discussion

@{
    // code block
    ViewData["Title"] = "Discussion Details";
}
<body class="DiscussionDetails">
    <div class="row d-flex justify-content-center">
        <div class="col-md-8  shadow p-3 mb-3 bg-white rounded">
            <img src="/photos/@Model.ImageFilename" alt="@Model.Content" class="img-fluid" />
        </div>


        <div class="col-md-8 bg-white rounded">
            <table class="table">
                @* <tr> *@
                @*     <td> *@
                @*         @Html.DisplayNameFor(model => @model.PhotoId) *@
                @*     </td> *@
                @*     <td> *@
                @*         @Html.DisplayFor(model => model.PhotoId) *@
                @*     </td> *@
                @* </tr> *@
                <tr>
                    <td>
                        <i class="fa-solid fa-l fa-camera" style="color: #B197FC;"></i> @Html.DisplayFor(model => model.Title)
                    </td>
                </tr>
                <tr>
                    <td>
                        <i class="fa-solid fa-l fa-circle-info" style="color: #B197FC;"></i> @Html.DisplayFor(model => model.Content)
                    </td>
                </tr>
                @* <tr> *@
                    @* <td> *@
                        @* Username display *@
                        @* <i class="fa-solid fa-l fa-location-dot" style="color: #B197FC;"></i> @Html.DisplayFor(model => model.Username) *@
                    @* </td> *@
                @* </tr> *@
                <tr>
                    <td>
                        <i class="fa-solid fa-l fa-calendar-day" style="color: #B197FC;"></i> @Html.DisplayFor(model => model.CreateDate)
                    </td>
                </tr>
                <tr>
                    <td>
                        <i class="fa-solid fa-tags fa-lg" style="color: #B197FC;"></i>
                        @if (Model.Comments != null && Model.Comments.Any())
                        {
                            @foreach (var tag in Model.Comments)
                            {
                                @* <span class="badge bg-success">@comment.Name</span> *@
                            }
                        }
                        else
                        {
                            <p>No tags available.</p>
                        }
                    </td>

                </tr>
            </table>
        </div>
    </div>
</body>

